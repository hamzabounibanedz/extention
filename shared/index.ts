/**
 * Shared types — used by backend, carriers package, and (via copy or build) Apps Script.
 */
export type {
  SheetColumnRef,
  InternalOrder,
  SendState,
  ShipmentStatus,
  SyncState,
  ColumnMapping,
  SavedSheetMapping,
  LicenseRecord,
  LicenseStatus,
  BusinessSettings,
  CarrierFeeRule,
  FeeRulesBlob,
  ShipmentBucket,
  BucketCounts,
  StatsRates,
  StatsSheetDateFilter,
  StatsSheetComputeResult,
} from './types/index.js';
